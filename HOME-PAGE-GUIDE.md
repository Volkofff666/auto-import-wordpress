# 🏠 Руководство по статической главной странице

## ✅ Готовая статическая главная страница создана!

Я создал полностью готовую главную страницу со всеми секциями в стиле проекта.

## 📋 Как использовать

### Шаг 1: Скачайте обновлённую тему

```bash
wget https://github.com/Volkofff666/auto-import-wordpress/archive/refs/heads/main.zip
unzip main.zip
```

Или скачайте через браузер:
- [Скачать ZIP](https://github.com/Volkofff666/auto-import-wordpress/archive/refs/heads/main.zip)

### Шаг 2: Обновите тему

**Через FTP:**
1. Удалите `/wp-content/themes/auto-import/`
2. Загрузите новую версию из `themes/auto-import/`

**Через админку:**
1. `Внешний вид` → `Темы`
2. Удалите старую тему Auto Import
3. Загрузите новую версию (заархивировав папку `themes/auto-import/` в zip)
4. Активируйте

### Шаг 3: Создайте главную страницу

1. **Создайте новую страницу:**
   - `Страницы` → `Добавить новую`
   - Название: "Главная" (или любое другое)

2. **Выберите шаблон:**
   - В правой панели найдите **"Шаблон страницы"**
   - Выберите: **"Home Page (Static)"**
   
3. **Опубликуйте:**
   - Нажмите `Опубликовать`

4. **Установите как главную:**
   - `Настройки` → `Чтение`
   - Выберите "Статическая страница"
   - В "Главная страница" выберите созданную страницу
   - Сохраните изменения

### Шаг 4: Настройте контент (опционально)

**Настройки для Hero секции:**
```
Auto Import → Настройки
```
Добавьте опции:
- `aic_hero_title` - заголовок героя
- `aic_hero_subtitle` - подзаголовок

Или отредактируйте напрямую в файле:
```
themes/auto-import/page-templates/template-home.php
```
Найдите строки с `get_option('aic_hero_title', ...)` и измените текст по умолчанию.

## 🎨 Что включает главная страница

### 1. Hero Section (Главный баннер)
- Большой заголовок и подзаголовок
- Две кнопки: "Смотреть каталог" и "Оставить заявку"
- Градиентный фон в фирменных цветах
- Полностью адаптивный дизайн

### 2. Trust Bar (Преимущества)
- 4 блока с иконками:
  - Гарантия качества
  - Быстрая доставка
  - Полное сопровождение
  - Опытная команда
- SVG иконки
- Сетка адаптируется под экран

### 3. Featured Cars (Популярные автомобили)
- Автоматически показывает 6 последних автомобилей из каталога
- Только с галочкой "Показывать в каталоге"
- Красивые карточки с фото, ценой, годом, пробегом
- Кнопка "Смотреть все автомобили" ведёт в каталог

### 4. How It Works (Как мы работаем)
- 5 шагов процесса покупки
- Нумерованные круги
- Понятное описание каждого этапа
- Серый фон для контраста

### 5. Contact Form (Форма заявки)
- Полностью рабочая форма с AJAX
- Поля: Имя*, Телефон*, Email, Бюджет, Комментарий
- Отправка через REST API в WordPress
- Email уведомления админу
- Красивые сообщения об успехе/ошибке
- Контактная информация слева от формы

## 📱 Адаптивность

Все секции полностью адаптивны:
- Десктоп: полная ширина, все элементы видны
- Планшет: сетки уменьшаются, 2 колонки
- Мобильный: 1 колонка, кнопки на всю ширину

## 🎨 Кастомизация

### Изменить цвета

Откройте `themes/auto-import/assets/css/tokens.css`:

```css
:root {
  --color-primary: #0066FF;        /* Основной цвет */
  --color-primary-hover: #0052CC;  /* При наведении */
  --color-secondary: #10B981;      /* Второстепенный */
  --color-accent: #FF6B35;         /* Акцентный */
  /* ... */
}
```

### Изменить тексты

Откройте `themes/auto-import/page-templates/template-home.php` и найдите нужную секцию.

**Например, изменить заголовок Trust Bar:**
```php
<h3 class="trust-bar__title">Ваш новый заголовок</h3>
```

### Изменить количество автомобилей

В файле `template-home.php` найдите:
```php
'posts_per_page' => 6,  // Измените на нужное число
```

### Добавить свой фон в Hero

1. Загрузите изображение в медиабиблиотеку
2. В `themes/auto-import/assets/css/home-page.css` найдите `.hero`
3. Добавьте:
```css
.hero {
  background-image: url('путь/к/вашему/изображению.jpg');
  background-size: cover;
  background-position: center;
}
```

## 🚀 Что дальше

### 1. Добавьте автомобили

```
Автомобили → Добавить новый
```
- Заполните все поля (цена, год, пробег, VIN)
- Загрузите фотографии
- ✅ **Обязательно** отметьте "Показывать в каталоге"
- Опубликуйте

Добавьте минимум 3-4 автомобиля чтобы главная выглядела наполненной.

### 2. Настройте таксономии

```
Автомобили → Марки
```
Добавьте: Toyota, BMW, Mercedes, Audi и т.д.

```
Автомобили → Статус
```
Добавьте:
- В наличии
- В пути
- Под заказ
- Продан

### 3. Заполните контакты

```
Auto Import → Настройки
```
- Телефон компании
- Email для уведомлений о заявках
- Адрес
- График работы

Эти данные автоматически появятся в форме заявки.

### 4. Создайте меню

```
Внешний вид → Меню
```
Добавьте пункты:
- Главная → ссылка на главную страницу
- Каталог → `/cars/`
- О компании → создайте страницу
- Контакты → создайте страницу

Назначьте меню на позицию "Primary Menu".

### 5. Добавьте виджеты в футер

```
Внешний вид → Виджеты
```

**Footer 1:**
- Виджет "Текст"
- Добавьте описание компании и логотип

**Footer 2:**
- Виджет "Меню навигации"
- Выберите созданное меню

**Footer 3:**
- Виджет "Текст"
- Добавьте контактную информацию

## 📊 Проверка работы формы

1. **Отправьте тестовую заявку** с главной страницы
2. **Проверьте что появилась в админке:**
   - `Заявки` → должна быть новая запись
3. **Проверьте email:**
   - На email из настроек должно прийти уведомление
4. **Если не работает:**
   - Проверьте что REST API доступен: откройте `/wp-json/aic/v1/`
   - Проверьте консоль браузера (F12) на ошибки

## 🎨 Примеры настройки

### Пример 1: Изменить градиент Hero

`themes/auto-import/assets/css/home-page.css`:
```css
.hero {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}
```

### Пример 2: Добавить видео в фон Hero

`themes/auto-import/page-templates/template-home.php`:
```php
<section class="hero">
    <video autoplay muted loop class="hero__video">
        <source src="<?php echo get_template_directory_uri(); ?>/assets/video/hero.mp4" type="video/mp4">
    </video>
    <!-- остальной код -->
</section>
```

### Пример 3: Изменить шрифты

`themes/auto-import/functions.php`:
```php
wp_enqueue_style('ai-fonts', 'https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&display=swap', [], null);
```

Затем в `tokens.css`:
```css
:root {
  --font-family: 'Montserrat', sans-serif;
}
```

## ❓ Часто задаваемые вопросы

### Как добавить ещё одну секцию на главную?

Откройте `template-home.php` и добавьте новую секцию между существующими:

```php
<!-- Ваша новая секция -->
<section class="my-section section">
    <div class="container">
        <h2>Заголовок секции</h2>
        <p>Контент секции</p>
    </div>
</section>
```

### Как убрать секцию?

Просто удалите или закомментируйте соответствующий блок в `template-home.php`:

```php
<?php /*
<section class="how-it-works">
    <!-- ... -->
</section>
*/ ?>
```

### Форма не отправляется, что делать?

1. Проверьте консоль браузера (F12) → вкладка Console
2. Проверьте что REST API работает: откройте `/wp-json/aic/v1/`
3. Проверьте что плагин Auto Import Core активен
4. Проверьте логи: `/wp-content/debug.log`

### Как сделать другой дизайн Hero секции?

Отредактируйте CSS в `home-page.css`. Например, сделать темнее:

```css
.hero {
  background: #1a202c;
}

.hero__overlay {
  background: rgba(0, 0, 0, 0.5);
}
```

## 📞 Поддержка

Если возникли вопросы:
1. Проверьте консоль браузера (F12) на ошибки
2. Проверьте `/wp-content/debug.log`
3. Создайте issue: [GitHub Issues](https://github.com/Volkofff666/auto-import-wordpress/issues)

---

✅ **Готово! Теперь у вас красивая главная страница в стиле проекта!**